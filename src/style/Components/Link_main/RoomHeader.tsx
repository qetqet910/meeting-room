import styled from 'styled-components';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'

const Header = styled.header`
    background: #f1f2f6;
    box-shadow: 0px 2px 10px #2f3542;
    z-index: 80;
`

export function RoomHeader(){
    return(
        <Header>
            
        </Header>
    )
}

